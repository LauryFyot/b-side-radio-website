# B-Side Site (incoming)

Ce dossier est reserve a la refonte du site principal (import depuis Lovable).

## Import recommande

1. Copier le contenu du repo `bside-groove-hub` dans ce dossier.
2. Ne pas copier son dossier `.git`.
3. Lancer `npm install` dans `apps/site`.
4. Tester avec `npm run site:dev` depuis la racine.

## Cible

- app publique React dans `apps/site`
- app admin React dans `apps/admin`
- scripts centralises a la racine du repo
